on:
  workflow_dispatch:

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Refresh AGHF KRACH Ratings
        run: |
          ./aghf.py download
          ./aghf.py update
          git config --global user.name "github-workflow"
          git config --global user.email "spam@users.noreply.github.com"
          git commit -am "AGHF Automated ranking update: $(date)"
          git push
